<!-- Content Header (Page header) -->
<section class="content-header">

<div class="row">
  <div class="col-xs-12">
        <%= form_tag("/words", method: "get", class: "") do %>
                        <div class="input-group input-group-sm">
                               <%= text_field_tag(:q, @q,class: "form-control") %>
                               <span class="input-group-btn">
                                <%= submit_tag("འཚོལ་བཤེར།", class: "btn btn-info btn-flat") %>
                               </span>
                          </div>
        <% end %>
  </div>
</div>

  <h1>
    ཚིག
    <small><%= link_to 'སྣོན་སྤེལ།', new_word_path %></small>
  </h1>
</section>

<!-- Main content -->
<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box">
        <div class="box-header">
          <!-- <h3 class="box-title">Hover Data Table</h3> -->
        </div><!-- /.box-header -->
        <div class="box-body">
          <table id="example2" class="table table-bordered table-hover">
            <thead>
              <tr>
                <th>མིང་།</th>
                <th>འགྲེལ་བཤད།</th>
                <th>གླེགས་བམ།</th>
                <th>མངོན་སྟོན།</th>
                <th>ལག་བསྟར།</th>
              </tr>
            </thead>
            <tbody>
              <% @words.each do |word| %>
                <tr>
                  <td><%= word.title %></td>
                  <td><%= word.description.truncate(50) %></td>
                  <td>
                    <% if !word.volumes.blank? %>
                        <%= word.volumes.title %>
                    <% end %>
                  </td>
                  <th>
                    <% if word.active? %>
                      ཡིན།
                    <% else %>
                      མིན།
                    <% end %>
                  </th>
                  <td>
                    <span><%= link_to 'ངོ་གདོང་།', word_path(word.id) %><span>
                    <span> | </span>
                    <span><%= link_to 'རྩོམ་སྒྲིག', edit_word_path(word.id) %><span>
                    <!-- <span> | </span>
                    <span><%= link_to 'delete', word_path(word.id), method: "DELETE", data: {confirm: 'Delete this data ? '} %><span> -->
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div><!-- /.box-body -->
      </div><!-- /.box -->
    </div><!-- /.col -->
  </div><!-- /.row -->
</section><!-- /.content -->
